<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Group Info</title>
  </head>
  <body>
    <div class="container">
      <h1><%= @group.name %></h1>
      <p><%= @group.desc %></p>
      <p>Created By: <%= @group.user.first_name%>
                     <%= @group.user.last_name%></p>
      <p>Members: </p>
      <form action="/join" method="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token%>">
        <input type="submit" name="" value="Join Group">
      </form>


      <%= link_to "Dashboard", user_path %> |
      <%= link_to "Log Out", "/logout", data: { method: :delete}%>
    </div>
  </body>
</html>
